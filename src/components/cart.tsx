import { Handbag } from 'phosphor-react'
import { CartButton } from '@styles/components/cart'

export function Cart() {
  return (
    <CartButton>
      <Handbag size={24} />
    </CartButton>
  )
}
